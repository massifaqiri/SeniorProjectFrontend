import React from 'react';

class CreateAccount extends React.Component {

    // Verify email DNE in DB

    // Render Form to take email & password
    render() {
        return (
            <p>Create an account</p>
        )
    }
}

export default CreateAccount;